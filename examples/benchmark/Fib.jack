class Main {
    function int main() {
        
        do Output.printf("%i\n", Main.fib(15));
        return 0;
    }
    function int fib(int n) {
        if (n < 2) { return 1; }
        return Main.fib(n - 1) + Main.fib(n - 2); 
    }
}
