class Main {
    function void main() {
        var int a;
        let a = Main.fib(1); 
        return;
    }
    function int fib(int a) {
        if (a < 2) { return 1; }
        return Main.fib(a-1) + Main.fib(a-2);
    }
    
}
